# Credenciales de la base de datos PostgreSQL del stack local de SonarQube.
# Copia este archivo a `sonarqube/.env` y ajusta los valores antes de levantar el stack.
SONAR_DB_USER=sonar
SONAR_DB_PASSWORD=cambia_esta_contrasena